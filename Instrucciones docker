ANTES DE NADA
docker stop $(docker ps -a -q)
docker rm $(docker ps -a -q)
docker rmi $(docker images -q)
docker volume prune

EMPIEZA
1. docker build . -t mariadb:1
2. docker volume create maria-db-volume
3. docker run -d -p 33061:3306 --name mariadb-db  --mount src=mariadb-volume,dst=/var/lib/mysql mariadb:1
4. docker exec -it mariadb-db mysql -p <-- contraseña:123
    mariadb> COPIAR PEGAR sql
    mariadb> chequear si esta ok
    mariadb> exit;
5. docker inspect mariadb-db